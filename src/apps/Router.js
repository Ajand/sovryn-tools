import Header from "./common/Header";
import Revocation from "./revocation";

const Router = () => {
  return (
    <div>
      <Header />
      <Revocation />
    </div>
  );
};

export default Router;
